# PostgreSQL Settings
PG_HOST=
PG_PORT=
PG_USER=
PG_PASSWORD=
PG_DB=
PG_SCHEMA=
# # Optional: Full database URL (overrides above if set)
# # ENSURE TO USE SAFE URL ENCODING FOR PASSWORD BEFORE PLACING IN THE FINAL URL (e.g.: @ becomes %40)
# PG_DATABASE_URL=


# Model Settings
TRANSFORMERS_OFFLINE=
HF_HUB_OFFLINE=
ENABLE_MODEL_WARMUP=
ENABLE_ASYNC_WARMUP=
WARMUP_BATCH_SIZE=


# Server Settings
HOST=
PORT=
DEBUG=
LOG_LEVEL=


# Security
# Dev settings
COOKIE_DOMAIN=
COOKIE_SECURE=false


# Pydantic-settings needs to convert a single string value from the .env file into a Python list (List[str]).
# The quotes tell the dotenv loader: "Treat everything inside these quotes as a single literal string value."
CORS_ORIGINS='["", ""]'


ENABLE_HSTS=


# # Production settings
# CORS_ORIGINS=["https://app.example.com"]
# COOKIE_DOMAIN=".example.com"
# COOKIE_SECURE=true
# ENABLE_HSTS=true


MAX_CONCURRENT_LLM_REQUESTS=
PROVIDER_CIRCUIT_BREAKER_THRESHOLD=
PROVIDER_CIRCUIT_BREAKER_TIMEOUT=


# Context and Response Limits
MAX_CONTEXT_LENGTH=
MAX_RESPONSE_TOKENS=


# Local LLM
OLLAMA_BASE_URL=


# Cloud providers
ENABLE_CLOUD_FALLBACK=
ENABLE_QUERY_CACHE=


CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
OPENROUTER_API_KEY=


# Optional: Premium fallback
DEEPSEEK_API_KEY=
OPENAI_API_KEY=


# Critical Security Settings
JWT_SECRET_KEY=
ALLOW_ADMIN_CREATION=
INITIAL_ADMIN_EMAIL=
INITIAL_ADMIN_PASSWORD=
MFA_ENCRYPTION_KEY=


# Authentication Settings
DEFAULT_AUTH_METHOD=
SESSION_TIMEOUT_HOURS=
MAX_FAILED_LOGIN_ATTEMPTS=
ACCOUNT_LOCKOUT_MINUTES=


# Email services
SENDGRID_API_KEY=
FROM_EMAIL=
FRONTEND_URL=


# Password Policy
PASSWORD_MIN_LENGTH=
PASSWORD_REQUIRE_UPPERCASE=
PASSWORD_REQUIRE_LOWERCASE=
PASSWORD_REQUIRE_DIGIT=
PASSWORD_REQUIRE_SPECIAL=


# SSO Settings (for future use)
OIDC_ENABLED=
SAML_ENABLED=


# Frontend (SolidJS/Vite): During the npm run build or npm run dev process, 
# Vite looks only for variables prefixed with VITE_. It takes those values and literally 
# bakes them into your final JavaScript bundles. Variables like DATABASE_URL are completely 
# ignored and never leave your development machine or build server.


VITE_APP_PORT=
VITE_API_BASE_URL=
VITE_APP_NAME=DOX